# Copy this file to .env (DO NOT commit secrets).
# Required production environment variables for Render or other hosts
# Security: keep values secret and do not commit .env with real secrets.

# Flask/debug
FLASK_DEBUG=0

# Flask entrypoint (used by some deploy shells and manage scripts)
# e.g. FLASK_APP=src/app.py
FLASK_APP=src/app.py

# Database (SQLAlchemy URL). For Render use the Postgres connection string provided
# Example (postgres): postgresql://user:password@host:5432/dbname
# Note: Render may return a URL that starts with postgres:// — SQLAlchemy accepts both formats
DATABASE_URL=

# JWT secret for API tokens - use a long random string (32+ chars)
JWT_SECRET_KEY=

# Flask session secret for server-side sessions - must be secret in production
FLASK_APP_KEY=

# Frontend origin for CORS (set to your frontend site URL)
FRONTEND_URL=

# Frontend build-time environment (Vite/React)
# If you use Vite, set the public API endpoint here so the built frontend knows where to call
VITE_API_URL=https://your-api.example.com
VITE_BACKEND_URL=https://your-api.example.com

# Mail configuration (optional, required for email features)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_DEFAULT_SENDER=

# Optional: configure number of Gunicorn workers (default used by Procfile if unset)
GUNICORN_WORKERS=4

# Observability / optional
# SENTRY_DSN= (optional)

# Security note:
# - Keep this file out of source control when it contains real secrets.
# - Use Render's Environment / Secrets UI to store production values.
